## Funcion principal
   Busca en el archivo 'vibra_challenge.csv' la información solicitada del siguiente querstring: http://localhost:5000/search?name=<string>&city=<string>&quantity=<int>
   
### Correr aplicación con docker compose
    docker-compose up --build